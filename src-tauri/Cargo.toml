[package]
name = "exoplanets-rip"
version = "1.0.0"
description = "Система расчёта массы экзопланет"
edition = "2021"

[lib]
name = "exoplanets_rip_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = ["codegen"] }

[dependencies]
tauri = { version = "2", features = [] }
